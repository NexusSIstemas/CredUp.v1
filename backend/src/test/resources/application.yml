spring:
  datasource:
    url: jdbc:h2:mem:credup;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  jwt:
    secret: test-secret-with-at-least-thirty-two-characters
    expiration-minutes: 60
  cors:
    allowed-origin: http://localhost:5173
